/*
  Warnings:

  - This migration has been manually emptied because the columns were already applied.
  - The previous attempt failed halfway (applying Form columns but failing on discordId).
  - Since SQLite saves changes immediately, the Form columns exist.
  - This empty file allows Prisma to mark the migration as "done" without errors.
*/

-- No operations needed.
